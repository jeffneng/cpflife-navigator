streamlit>=1.38
plotly>=5.24
openai==1.54.4
httpx==0.27.2
protobuf>=3.20,<7

# RAG pipeline (Policy Explainer): load -> split -> store -> retrieve -> output
# Embeddings use our own openai==1.54.4 client (see rag/embeddings.py) rather
# than the langchain-openai package, which requires openai>=2.45,<4.0 — every
# version in that range ships a broken bundled HTTP client (upstream bug,
# unrelated to this app; see the comment in rag/embeddings.py).
langchain==1.4.0
langchain-chroma==1.1.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
chromadb==1.5.9
docx2txt==0.9

# Streamlit Community Cloud's Linux base image ships an SQLite older than
# Chroma's minimum (3.35+). This provides a modern SQLite to swap in via the
# shim in cpf_life/sqlite_shim.py. No macOS wheels exist, so it's Linux-only
# (harmless to omit locally — your system SQLite is already new enough).
pysqlite3-binary; sys_platform == "linux"
